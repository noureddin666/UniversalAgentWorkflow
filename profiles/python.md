# Python profile

Detect the environment and dependency manager from repository files. Respect supported Python versions, type-checking policy, formatting, linting, and test framework. Keep import-time side effects controlled and configuration external to business logic. Use repository-declared commands.

