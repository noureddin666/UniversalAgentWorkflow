# .NET profile

Detect solution and project structure before choosing commands. Respect repository language version, analyzers, nullable settings, package management, and test framework. Propagate cancellation through asynchronous I/O, use structured logging, and keep domain logic independent of persistence and hosting details. Prefer repository-declared commands over generic `dotnet` commands.

