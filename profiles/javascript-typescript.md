# JavaScript and TypeScript profile

Detect the package manager from its lockfile and never mix package managers. Respect existing compiler, lint, formatting, runtime, and framework conventions. Keep server and client trust boundaries explicit. Verify types, tests, and production build through repository-declared commands.

