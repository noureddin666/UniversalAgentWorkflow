# Bugs and discoveries

- `bugs/` records reproducible incorrect behavior and regressions.
- `discoveries/` records facts, constraints, risks, opportunities, and architectural observations found during work.

Each finding links to owning scopes and affected specs. Keep raw evidence beside the finding rather than hiding it in conversation history.
