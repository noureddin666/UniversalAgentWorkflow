# Scope knowledge bases

Create business or application scopes through the workflow CLI. Each scope owns a profile, current state, decisions, and material change history. These artifacts are automatically inherited by nested scopes and selected by path routing.
