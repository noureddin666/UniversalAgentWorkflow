# الدليل العربي — Universal Agent Workflow

دليل عملي مختصر: شو هاد الشي، كيف بتنصّبه، وكيف بتستخدمه يوميًا.
ابدأ من [README.md](README.md) لنظرة سريعة وتنصيب، أو استخدم هذا الدليل للخطوات العملية بالعربي. الشرح الكامل والمرجع المتقدم بالإنجليزي في [MANUAL.md](MANUAL.md).

---

## 1. شو هو بالضبط

**نظام حفظ للحقائق اللي بحتاجها الوكيل، وبوابات بتفحص شغله.**

بينصّب مجلد `.agent/` جوّا الريبو تبعك، وأداة Python صغيرة بتقرأه. المجلد بيحمل الأشياء اللي **ما بيقدر** الوكيل يستنتجها من الكود: شو الهدف من المنتج، أي أوامر مثبت إنها بتشتغل، أي متطلبات انعتمدت ومين اعتمدها، أي حدود معمارية لازم تضل صامدة، وشو اللي انتعلّم من شغل سابق.

هو **مش**: موديل، ولا وكيل، ولا مكتبة برومبتات، ولا بيلد سيستم، ولا بديل عن نظام التذاكر، ولا بيكتب كود عنك.

**المشكلة اللي بيحلها:** بتفتح جلسة جديدة، الوكيل بينسى كل شي من الجلسة اللي قبل. بيخترع أمر بيلد ما موجود، بيناقض قرار اتفقتوا عليه الشهر الماضي، وبيحط ملف بمكان غلط. المعلومات اللي بتمنع هالحكي عاشت بالمحادثة — والمحادثة راحت.

---

## 2. المتطلبات

- مشروع موجود (أو مجلد فاضي إذا رح تبدأ من الصفر)
- Python 3.9 أو أحدث
- بس. ما بينصّب أي اعتمادية لمشروعك وما بيعدّل ولا سطر من كودك.

---

## 3. التنصيب — أمرين

الأول بتشغّله من **ريبو الفريمورك**، والثاني من **مشروعك**:

```shell
python scripts/install_workflow.py ../your-project
cd ../your-project
python .agent/workflow.py setup --ask
```

`setup` بيقرأ الريبو وبيعبّي كل إشي بيقدر يثبته من الملفات. بعدها `--ask` بيسألك الأسئلة القليلة اللي ما في ملف بيجاوب عليها: الهدف، المستخدمين، قيود الأمان. اضغط Enter لتتخطى أي سؤال. لما الوكيل هو اللي بيشغّل الإعداد، شغّله بدون `--ask`.

بعد التنصيب استعمل الغلاف المناسب لصدفتك لأنه بيلاقي مفسّر بايثون المتاح لحاله: على ويندوز `.agent\workflow.cmd <command>`، وعلى ماك/لينكس `./.agent/workflow.sh <command>`. ملف `workflow.ps1` بيحتاج `pwsh -NoProfile -ExecutionPolicy Bypass -File` لما تكون سياسة التنفيذ مقيّدة.
إذا `python` مش الاسم عندك، استعمل `python3`.

| الخطوة | شو بتعمل | شو **ما** بتعملها |
|---|---|---|
| `install_workflow.py` | بتضيف `AGENTS.md` و `.agent/` وملف CI | ما بتكتب فوق ولا ملف — بتوقف كليًا إذا `AGENTS.md` أو `.agent/` موجودين |
| `bootstrap` | بتكتب `.agent/ONBOARDING.md`: أوامر وحقائق ونطاقات مقترحة، كل واحد مع دليله | ما بتشغّل ولا أمر |
| `adopt` | بتكتب الحقائق والأوامر اللي إلها دليل قوي بـ `PROJECT.md` و `COMMANDS.md` | ما بتكتب فوق قيمة كتبها إنسان، وما بتكتب تخمين مبني على العُرف بس |
| `local-init` | بتبني `.agent/INDEX.json` و `.agent/dashboard.html` | ما بتلمس الكود |
| `sync-entrypoints` | بتعمل ملفات إشارة للأدوات اللي ما بتقرأ `AGENTS.md` (Claude، Copilot، Cursor، Gemini) | إذا الملف موجود بتتركه زي ما هو |
| `doctor` | بتقلك شو ناقص أو فاضي أو منحرف | القوالب الفاضية تحذير مش فشل |

> **مشروعك فيه `AGENTS.md` أو `CLAUDE.md` من قبل؟** التنصيب بيوقف بدل ما يمسحهم. نصّب على مجلد مؤقت وبعدين ادمج بإيدك. و`bootstrap` رح يقلك أي ملفات تعليمات لقاها عشان توحّدهم مع `AGENTS.md` بدل ما يصير عندك مصدرين للحقيقة.

---

## 4. `bootstrap` و `adopt` — الأهم بالموضوع

التنصيب لحاله بيعطيك **هيكل فاضي مش معرفة**. `PROJECT.md` كله `TODO` و`COMMANDS.md` جدول فاضي. الوكيل اللي بيفتح المشروع بهاي اللحظة ما بيتعلّم إشي جديد.

`bootstrap` بيقرأ الريبو وبيكتب **ملف واحد بس** هو `.agent/ONBOARDING.md`، وما بيشغّل ولا أمر. بعده `adopt` بياخد الاقتراحات اللي إلها دليل قوي وبيكتبها بـ `PROJECT.md` و `COMMANDS.md`، وكل قيمة بيكتب جنبها الملف اللي طلعت منه. `setup` بيعمل الاثنين مع بعض.

**شو بيقرأ → شو بيقترح:**

| بيقرأ | بيقترح |
|---|---|
| الـ manifests والـ lockfiles | التقنيات، مدير الحزم، أي بروفايل ينطبق |
| سكربتات `package.json`، أهداف Makefile، ملفات الحلول، manifests بايثون | صفوف مرشّحة لـ `COMMANDS.md` |
| الاعتماديات، الـ SDKs، إصدارات الـ runtime، `README.md`، ملفات النشر، هيكل المجلدات | حقائق لـ `PROJECT.md`: الهدف، شكل النظام، المسارات، الخدمات الخارجية، بيئة النشر، الإصدارات |
| ملفات CI | نفس الشي، بس الأمر مذكور بخطوة CI؛ نجاحه غير مثبت من قراءة الملف وحده |
| المجلدات اللي عندها manifest خاص فيها | نطاقات (scopes) وموديولات معمارية مرشّحة |
| `CLAUDE.md` و`GEMINI.md` وملفات Copilot و Cursor الموجودة | تعليمات لازم توحّدها مش تكرّرها |

**كل اقتراح بيجي مع الملف اللي طلع منه ودرجة ثقة:**

| اللِيبل | معناه | الثقة |
|---|---|---|
| `ci` | ظاهر في بايبلاين CI بالريبو | دليل على التصريح بالأمر، مش على نجاح تشغيله |
| `declared` | معرّف في manifest بالريبو | موجود، بس ما حدا أثبت إنه بينجح |
| `documented` | مكتوب بالـ README تبع الريبو | صحيح قد ما الـ README محدّث |
| `observed` | ظاهر بملفات ومجلدات الريبو | موجود اليوم |
| `derived` | مستنتج من عُرف التقنية بس | الأكثر عرضة للغلط، و`adopt` ما بيكتبه |

**ما في ولا اقتراح موسوم "متحقق منه"** — التحقق يعني التشغيل، وهاي خطوتك انت أو الوكيل.

الموديولات المقترحة دايمًا بتيجي بـ `"may_depend_on": []` فاضية، لأن اتجاه الاعتمادية المسموح **قرار تصميمي** — السكان بيشوف المجلدات، ما بيشوف النية.

بتقدر تعيد توليده بأي وقت بـ `--force`، أو تقرأه JSON بـ `--json`.

---

## 5. الشي الوحيد اللي بيضل عليك

`setup` بيعبّي لحاله مسارات الكود والتستات والتوثيق، والفريموركات وإصداراتها، وقواعد البيانات والخدمات، وبيئة النشر، والمسارات المحمية، وكل أمر معرّف بالريبو. اللي بيضل:

1. **الأسئلة اللي ما في ملف بيجاوبها**: المستخدمين، قدرات النظام، الحدود المقصودة، قيود الأمان. `setup --ask` بيسألك إياهم بالترمينال، أو عدّلهم بـ `PROJECT.md` مباشرة. اللي ما حدا بيعرفه اكتب فيه `unknown` حرفيًا.
2. **شغّل كل أمر بـ `COMMANDS.md` مرة وحدة**. `adopt` بيكتب الأوامر المعرّفة بالريبو بس ما بيشغّلها، وأمر غلط أسوأ من صف فاضي لأن الوكيل بيثق فيه لشهور.

`adopt` ما بيكتب أبدًا فوق قيمة كتبتها إنت، فبتقدر تعيد تشغيله بأمان كل ما تغيّر الريبو.

**الوكيل بيقدر يعمل الباقي عنك.** البرومبت الجاهز مطبوع بآخر `.agent/ONBOARDING.md`، وخلاصته:

```text
adopt عبّى كل إشي إله دليل. كمّل الباقي:
- شغّل كل أمر بـ COMMANDS.md مرة، وصلّح أو احذف اللي بيفشل.
- لكل حقل بـ PROJECT.md لسا TODO، لاقي جوابه بالريبو واذكر الملف.
- أي إشي ما بتقدر تثبته من ملف يصير "unknown"، والهدف والمستخدمين اسألني عنهم.
- ما تعدّل كود التطبيق أثناء الـ onboarding.
```

---

## 6. السيناريوهات

### مشروع قائم (الحالة الشائعة)

نصّب → `setup --ask` → جاوب الأسئلة → شغّل الأوامر مرة. هاي الحالة اللي بتستفيد أكثر إشي من السكان.

**نصيحة:** ما تعرّف كل النطاقات اللي اقترحها السكان من أول يوم. الحقائق بس (`PROJECT.md` و `COMMANDS.md` و `DECISIONS.md`) بتغطي 80% من القيمة. النطاقات بتيجي لما فعلاً يصير عندك مناطق بدها سياقات مختلفة.

### مشروع من الصفر

`bootstrap` رح يرجّع `mode: greenfield` ويوقف عن الاقتراح، لأن ما في أدلة يقرأها. الترتيب **بينقلب**:

| مشروع قائم | من الصفر |
|---|---|
| تستخرج الحقائق من الكود | تصرّح بالنية قبل الكود |
| `COMMANDS.md` من اللي الريبو أصلاً بيشغّله | `COMMANDS.md` بعد أول build ناجح |
| قواعد معمارية بتركّبها على الموجود | قواعد معمارية بتكتبها والمجلدات لسه فاضية |
| specs لما حدا لازم يعتمد | specs من الأول — انت أصلاً عم تكتب النية |

تعريف الحدود في `architecture.json` وقت الريبو فاضي بيكلّف دقايق وبيمنع الانحراف اللي غالي تصلّحه بعدين. هاي أعلى قيمة ممكن تعملها بأول يوم، والمرة الوحيدة اللي بتكون شبه مجانية. أعد `bootstrap --force` بعد ما يصير في كود.

### Monorepo

كل مجلد عنده manifest بيطلع نطاق منفصل مع أوامره. اللي بتقرر تخليه، اعطيه أوامر تحققه واعتمادياته:

```shell
python .agent/workflow.py create-scope api --title "Public API" --kind application --path apps/api --verify "tests=pnpm --filter api test"
python .agent/workflow.py create-scope web --title "Web" --kind application --path apps/web --depends-on api
```

بعدها `impact` بيوسّع أي تغيير للمستهلكين اللي تحته وبيرجّع **بس** التحقق اللي هالتغيير بيحتاجه.

### مشروع راكد رجعتله بعد شهور

الحقائق المكتوبة بتتعفّن. قبل ما تثق فيها:

```shell
python .agent/workflow.py doctor          # شو صار غير موصوف
python .agent/workflow.py check-index     # هل الفهرس لسه مطابق لمصادره
python .agent/workflow.py bootstrap --force
```

وبعدين قارن `ONBOARDING.md` الجديد مع اللي مكتوب بـ `COMMANDS.md`. `adopt` بيعبّي أي حقل رجع `TODO`، بس ما بيصلّح قيمة قديمة. هاي عليك.

---

## 7. الاستخدام اليومي

افتح المشروع بأداة وكيل بتقرأ `AGENTS.md`، واطلب شغلك بلغة عادية:

```text
خلي الزبون يقدر يلغي الطلب قبل ما يبلش التجهيز.
```

`AGENTS.md` بيوجّه الوكيل إنه: يحل المسارات اللي رح يلمسها، يقرأ **بس** السياق المطابق، ينفّذ أصغر تغيير متماسك، يتحقق بالأوامر المصرّح فيها، وبعدين يقول شو تحقق منه فعلاً وشو لسه غير مؤكد.

أوامر بتستعملها أثناء الشغل:

```shell
python .agent/workflow.py route-changes           # أي نطاقات بتلمسها تعديلاتي الحالية؟
python .agent/workflow.py route src/orders.py --intent bugfix
python .agent/workflow.py route src/orders.py --task "أصلح تكرار الطلبات"
python .agent/workflow.py context-budget src/orders.py --intent bugfix
python .agent/workflow.py handoff validate
python .agent/workflow.py agent-budget --agents 6 --concurrency 6 --high-effort 1
python .agent/workflow.py local-check             # specs + routing + معمارية + فهرس + أثر
python .agent/workflow.py local-check --execute   # ونفّذ كمان أوامر تحقق النطاقات المتأثرة
python .agent/workflow.py build-index             # حدّث الفهرس بعد إضافة نطاق أو spec
python .agent/workflow.py dashboard               # أعد توليد .agent/dashboard.html
```

للوصول لقسم واحد من مستند طويل بدون ما تقرأه كله:

```shell
python .agent/workflow.py outline MANUAL.md                     # معرّفات الأقسام وعدد أسطرها
python .agent/workflow.py read MANUAL.md --section <section-id> # هالقسم بس، ومعه فهرس الملف كامل
python .agent/workflow.py find "تسليم المهام" --path docs/      # مراجع أقسام مرتّبة
```

معرّف القسم هو سلسلة عناوينه، مثل `user-manual/9-commands-grouped-by-intent`، والقسم بيحمل أقسامه
الفرعية معه. ما في فهرس مخزّن فما في إشي بيتقادم — الهيكل بينشتق من الملف كل مرة. و`find` لفظي
بيرتّب حسب كم كلمة تطابقت وكثافتها، وبيشيل أي قسم بيحتوي تطابق أضيق منه، فالجواب بيكون **أضيق قسم**
مش المستند كله. وكل `read` بترجع فهرس الملف مع القسم، لأن خطر القراءة الضيقة مش إنك تقرأ قليل — بل
إنك ما تعرف شو تخطّيت.

للمهام الأكبر من جلسة وحدة، شغّلها كسلسلة مهام حتى تاخد كل مهمة سياق نظيف:

```shell
python .agent/workflow.py run start <spec>        # ازرع التنفيذ من tasks.md
python .agent/workflow.py run next <spec>         # بريف مهمة وحدة بس
python .agent/workflow.py run complete <spec> <task> --code src/x.py --test tests/test_x.py     --evidence "..." --handoff "..."
python .agent/workflow.py run retry <spec> <task> # فكّ الحجب بعد ما تصلّح السبب
python .agent/workflow.py run replan <spec>       # تبنَّ خطة معادة الكتابة
python .agent/workflow.py run status <spec>
python .agent/workflow.py run report <spec>       # شو كلّفتك السلسلة فعلياً
```

`complete` بتفحص شغلتين قبل ما تتقدّم. الملفات المصرّح فيها بـ`--code` و`--test` لازم تكون موجودة
و**فعلاً اختلفت** عن وضعها لحظة ما بلشت المهمة — فمهمة ما انعمل شغلها ما بتقدر تمرّ على تحقق كان
ناجح أصلاً. وبعدين بيشتغل تحقق النطاق المالك. المهمة الفاشلة ما بتكتب دليل ولا تسليم، والسلسلة
بتوقف لما تستنفد `task_max_attempts`. استعمل `--allow-no-change` للمهمة اللي فعلاً ما بتعدّل إشي.
عند النجاح بينكتب رابط الـtraceability لحاله، فما تستدعي `link-requirement` مرة تانية لنفس المهمة. أعد كتابة `tasks.md` كل ما تعلّمت شي بيبطّل الخطة — الخطة
المتقادمة بتحجب `next` لحد ما `replan` تتبنّاها، والمهام المنجَزة بتحتفظ بأدلّتها.

نتائج التحقق بتتخزن تحت `.agent/runs/`، واللوحة في `.agent/dashboard.html` بتفتحها بالمتصفح مباشرة.

`route --intent` بدمج نوع المهمة مع ملكية المسار. إذا ما حددته، `--task` يستنتجه بشكل خفيف من
وصف عربي أو إنجليزي وبيعلّم النتيجة إنها مستنتجة. `context-budget` بقيس السياق قبل ما توسّعه.
ملف `.agent/HANDOFF.md` حالة مؤقتة: اقرأه لما تكون الحالة `active`، اكتب فوقه بدل الإضافة،
وامسحه بعد إنهاء المهمة بـ `handoff clear`.
`agent-budget` بفحص عدد الـagents والتوازي وعمليات high-effort قبل تشغيل workflow جماعي.

---

## 8. المستويات — شغّل اللي بتحتاجه بس

القرار مش "أشغّل الفريمورك كله ولا لأ"، القرار لكل مستوى لحاله:

| المستوى | شغّله لما | الكلفة |
|---|---|---|
| **1. الحقائق** — `PROJECT.md`, `COMMANDS.md`, `DECISIONS.md` | رح تفتح أكثر من جلسة وكيل على هالريبو | أمر `setup --ask` وكم جواب، مرة وحدة |
| **2. النطاقات** — `create-scope`, `route`, `impact` | الريبو فيه أكثر من منطقة محدودة | ~10 دقايق للنطاق |
| **3. الحدود** — `architecture.json` في CI | الكود بينزل أسرع ما بتراجعه، والبنية عم تنحرف | ~20 دقيقة لأهم حدين أو تلاتة |
| **4. المواصفات** — `create-spec` ودورة الحياة والتتبّع | في حدا غيرك لازم يعتمد أو يدقّق شو انبنى | طقوس حقيقية ومستمرة |

**لو انت لحالك مع وكيل على تطبيق صغير:** المستوى 1 و3 أهم إلك من فريق كبير، مش أقل — لأن الفريق عنده ذاكرة بشرية، وانت ذاكرتك هي بس اللي كتبته. المستوى 4 على الأغلب زيادة عليك.

**متى تتجاهل كل هاد الحكي:** سكربت لمرة وحدة، أو إذا ما رح تحافظ على الحقائق محدّثة (جدول أوامر فيه أمر مات أسوأ من جدول فاضي)، أو إذا الـ README أصلاً بيعمل الشغلة والوكيل فعلاً بيقرأه.

---

## 9. أخطاء شائعة

| الخطأ | ليش مشكلة | الصح |
|---|---|---|
| نسخ كل أوامر `ONBOARDING.md` لـ `COMMANDS.md` بدون تأكيد | الوكيل بيثق فيها بشكل أعمى، والغلط بينتشر لشهور | أكّد كل واحد، وارمِ اللي ما تأكّدته |
| تحويل `TODO` لتخمين معقول | تخمين مكتوب بيصير حقيقة بعد أسبوع | خلّيه `unknown` حرفيًا |
| تعريف 15 نطاق من أول يوم | صيانة بلا مقابل | نطاق لما فعلاً بدك توجّه الشغل غير |
| تعبئة `architecture.json` بتصنيف كامل | ما رح تحافظ عليه | أهم حدين أو تلاتة بس |
| استعمال `-Force` بمنصّب PowerShell عشان تتخطى رفض التنصيب | بيدهس إعدادات الوكيل الموجودة | نصّب بمجلد مؤقت وادمج بإيدك |
| تحديث الفريمورك بإعادة التنصيب | بيدهس شغلك | `python scripts/update_workflow.py ../your-project --migrate` |

---

## 10. الترقية والصيانة

```shell
python scripts/update_workflow.py ../your-project --migrate
```

بيبدّل حزمة الفريمورك المدمجة بس، وبيحافظ على كل شي مملوك للمشروع: النطاقات، المواصفات، الملاحظات، القرارات، والإعدادات. وبينقل النسخة القديمة لـ `.agent/backups/` قبل، وبيرجّعها إذا فشل الاستبدال.

بعد الترقية:

```shell
python .agent/workflow.py migrate
python .agent/workflow.py build-index
python .agent/workflow.py doctor
```

---

## 11. أولوية التعليمات

لما تتعارض التعليمات، الترتيب من الأقوى:

1. طلبك الصريح الحالي
2. حدود الأمان والصلاحيات
3. تعليمات المشروع والمتطلبات المعتمدة
4. بروفايلات التقنية المطابقة
5. افتراضيات الفريمورك

وتعليمة أعلى أولوية **ما بتصرّح لحالها** بعمليات تدميرية، أو تغييرات على الإنتاج، أو تواصل خارجي.

---

## 12. حدود معروفة

- `bootstrap` بيصنّف الأوامر بالاسم والموقع — ما بيقدر يعرف إذا سكربت اسمه `test` فعلاً بيختبر. عشان هيك كل صف بده تأكيد.
- قواعد المعمارية بتطابق البادئات المصرّح فيها، وما بتحل الـ aliases من `tsconfig.json` أو إعدادات الباندلر.
- `local-check --execute` بينفّذ الأوامر المعرّفة في `.agent/config/scopes.json` عبر الشل. تعامل مع هالملف كمدخل موثوق وراجع تغييراته بالكود ريفيو.
- أوامر التحقق مش معزولة (sandboxed)، وما عليها حدود موارد غير `command_timeout_seconds`.
