# Bug-fix workflow

Find or create the bug record and link it to owning scopes and affected specs. Establish expected versus observed behavior and reproduce when practical. Trace the failure to a cause rather than patching a symptom. Protect the failing behavior with an appropriate regression check when authorized. Make the narrowest coherent fix, verify both the original failure and nearby behavior, then resolve the bug with evidence.
