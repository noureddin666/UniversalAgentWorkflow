# Discovery workflow

Record a discovery when investigation reveals a durable fact, constraint, risk, opportunity, dependency, or architectural observation. Link it to its owning scopes and affected specs, preserve evidence, and distinguish observation from interpretation. If it changes accepted behavior, update the owning spec and scope change log before implementation. Resolve or dismiss it only with recorded evidence.
