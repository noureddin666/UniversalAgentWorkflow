# Diagnosis workflow

Gather evidence, reduce the search space, and distinguish confirmed facts from hypotheses. Use read-only checks by default. Record durable facts, constraints, risks, or opportunities as discoveries linked to their owning scopes and specs. Report the cause, evidence, impact, and viable fixes. Do not implement a fix unless the request includes implementation.
